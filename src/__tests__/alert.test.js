describe('Alert system tests', () => {
  it('Alert should show up once CPU is under heavy load', () => {

  });
  it('Alert should show up once CPU is back to normal', () => {

  });
  it('UI should display the previous alerts', () => {

  });
});
